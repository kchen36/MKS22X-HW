public class Merge{
    public Merge(){
    }
    public static void mergesort(int[] ary){
	if(ary.length == 1){
	}
	else{
	    int[] left,right;
	    left = new int[ary.length/2];
	    right = new int[ary.length - left];
	    for(int x = 0;x < left.length;x ++){
		    left[x] = ary[x]
	    }
	    for(int x = 0;x < right.length;x++){
		right[x] = ary[x + left.length];
	    }
	    mergesort(left);
	    mergesort(right);
	    int[] destination = new int[left.length + right.length]; 
	    merging(left,right,destination);
	}
    }
    public static void merging(int[]
}
